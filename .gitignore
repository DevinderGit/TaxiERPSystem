# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
dist
dist-ssr
*.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Node
node_modules

# Env files (must never be committed — anon key + service role key live here)
.env
.env.local
.env.*.local

# Supabase local artifacts
supabase/.branches
supabase/.temp

# Test artefacts (we don't run automated tests but keep the noise out)
coverage
