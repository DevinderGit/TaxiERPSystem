# Taxi ERP — Work Log

> Append-only change log. Every change I make goes here. Past entries are never edited or deleted; reverts are appended as new entries.

---

## 2026-09-16 17:05 IST — TAXI-001 — Initialize Vite + React 18 + TypeScript project

- What I changed (files created):
  - `package.json`, `package-lock.json` (Vite 5.4 + React 18.3 + TS 5.6 + ESLint 9 flat config + typescript-eslint)
  - `tsconfig.json` (project references), `tsconfig.app.json` (strict + bundler resolution), `tsconfig.node.json`
  - `vite.config.ts` (`@vitejs/plugin-react`, port 5173)
  - `eslint.config.js` (flat config — TS recommended + react-hooks + react-refresh)
  - `index.html` (`#root` mount, page title `Taxi ERP`, Vite favicon)
  - `src/main.tsx` (React 18 `createRoot` + `<StrictMode>`)
  - `src/App.tsx` (default Vite + React welcome component with counter, per Manual Test Plan step 4)
  - `src/index.css`, `src/App.css`, `src/vite-env.d.ts`
  - `src/assets/react.svg`, `public/vite.svg`
  - `.gitignore` (node_modules, dist, .env*, supabase/.branches, .vscode, OS junk)
  - `src/{components,hooks,services,panels,templates,lib,types}/.gitkeep` (placeholder markers per CLAUDE.md Quick Reference file layout)
  - `docs/worklog.md` (this file — created on first action per CLAUDE.md)
- Why: establish the minimal frontend skeleton the rest of M0..M14 builds on. Strict mode + project-references TS config + ESLint flat config + `.gitignore` covering `.env*` are the foundations every later ticket assumes.
- Manual test status:
  - Step 1 `npm run dev` — **PASS** (Vite ready in 175 ms on `http://localhost:5173/`)
  - Step 3 open the URL — **PASS by curl** (`/` → 200, HTML body contains `<title>Taxi ERP</title>`, `#root` div, Vite client + React Refresh injection, `/src/main.tsx` script tag; `/src/main.tsx` → 200, transformed JS shows `StrictMode` + `createRoot` + `App` import — **operator should open the browser to visually confirm both logos render with no console errors**)
  - Step 5 `Ctrl+C` stop — **PASS** (background task received SIGTERM, output ends with `[killed]`, no error trace)
  - Bonus — `npm run build` — **PASS** (`tsc -b` clean, `vite build` emits `dist/index.html` + 1.39 kB CSS + 144.38 kB JS / 46.95 kB gzip in 381 ms; no errors, no warnings)
  - Bonus — `npm run lint` — **PASS** (exit 0, no output)
  - Step 4 visual confirmation (default welcome page with logos + counter, no console errors) — **PENDING operator**: please open `http://localhost:5173/` in your browser and confirm the Vite + React logos render and clicking the counter increments the count. No automated tests by policy.
- Open questions for operator: none. Versions are locked at the latest stable compatible with the design spec (Vite 5, React 18, TS 5.6, ESLint 9). Two dev-only vulnerabilities were reported by `npm audit` — both are in transitive dev deps and acceptable at this stage.

## 2026-09-16 17:12 IST — TAXI-001 — Operator verification of Manual Test Plan step 4

- What changed: operator confirmed via browser that `http://localhost:5173/` shows the default Vite + React welcome page with both logos visible and the click counter increments. F12 console has no errors.
- Why: visual / interactive confirmation cannot be automated; the operator is the manual-test-plan executor for browser steps per CLAUDE.md.
- Manual test status: ALL 5 STEPS PASS (steps 1, 2/3, 5 verified by me via `npm run dev` + curl + SIGTERM; step 4 verified by operator).
- Open questions for operator: none.

## 2026-09-16 17:25 IST — TAXI-002 — Install core frontend dependencies

- What I changed (files):
  - `package.json` — added 9 runtime deps (`@hookform/resolvers`, `@react-pdf/renderer`, `@supabase/supabase-js`, `@tanstack/react-query`, `@tanstack/react-table`, `react-hook-form`, `react-router-dom`, `zod`, `zustand`) under `dependencies`; added `prettier` under `devDependencies`; added `lint:fix`, `format`, `format:check` scripts.
  - `package-lock.json` — regenerated by `npm install` (96 new packages, total 272).
  - `.prettierrc` — baseline config (semi, single quotes, trailing commas, 100-col, LF endings).
  - `.prettierignore` — excludes operator-authored docs (CLAUDE.MD, TaskList.md, worklog.md), harness config (`.claude/`), and JSON lockfiles.
  - `src/App.tsx`, `eslint.config.js` — auto-reformatted by `prettier --write` (whitespace only; no behaviour change).
- Why: spec lists these libs as the foundation for M0..M14 (TanStack Query for server state, Zustand for UI, RHF+Zod for forms, react-pdf for PDF generation in M11, Supabase for data, React Router for the lazy-loaded panels in TAXI-006). Prettier is in the spec's dev-deps list.
- Manual test status:
  - Step 2 (open `package.json`, see all required deps) — **PASS**. Verified via Node script: all 8 spec-listed deps present + `prettier`. Total deps + devDeps = 22 keys.
  - Step 3–4 (`npm run build`) — **PASS**. `tsc -b` clean, `vite build` emits `dist/` (144 kB JS / 47 kB gzip) in ~290 ms. Run twice — before and after prettier reformat — both green.
  - Step 5–6 (`npm run lint`) — **PASS**. Exit 0, no output. Run before and after prettier reformat — both green.
  - Bonus — `npm run format:check` — **PASS** ("All matched files use Prettier code style!") after `.prettierignore` + auto-reformat.
- Open questions for operator: none. `npm audit` reports 4 vulnerabilities (3 moderate, 1 high) — all in `@react-pdf/renderer`'s transitive deps (pdfjs / canvg). Acceptable for the dev phase; will revisit before M11 ships.

## 2026-09-16 17:55 IST — TAXI-003 — Set up Supabase CLI + local self-hosted stack

- What I changed (files created):
  - `supabase/config.toml` (415 lines, generated by `supabase init`, project_id=`TaxiERPSystem`, all 12 services enabled with default ports)
  - `supabase/.gitignore` (auto-created by init; ignores `.temp`, `.branches`)
  - `supabase/migrations/.gitkeep` (marker so the directory is tracked from day one — first real migration lands in TAXI-101)
  - `.env.local` — 5 keys: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`, `SUPABASE_DB_URL`. Confirmed gitignored.
- Side-effect (outside the repo): installed Supabase CLI v2.117.0 globally via `npm install -g supabase`. Pulled 11 Docker images and launched 12 containers (~3 GB RAM; ~6 minutes on first run).
- Why: provide the persistent DB / Auth / REST / Storage / Studio layer the rest of M0..M14 talks to. The CLI version is pinned only by the npm global — `config.toml` doesn't pin image versions; that's a future hardening step (TAXIs 1501-1510 will add pinned versions per System Design §2.3).
- Manual test status:
  - Step 1–2 `supabase start` — **PASS** (containers up after image pull; output JSON printed all keys + URLs).
  - Step 3–4 API URL response — **PASS by curl** (`http://127.0.0.1:54321/` → 404 with `{"message":"no Route matched with those values"}` JSON; `http://127.0.0.1:54321/rest/v1/` → 200 with full OpenAPI 2.0 schema document — PostgREST v16.2 confirmed alive; `apikey` header accepted). Browser visual confirmation **PENDING operator**.
  - Step 5–6 Studio URL — **PASS by curl** (`http://127.0.0.1:54323/` → 307 redirect to `/project/default` — SPA loading correctly). Browser visual confirmation of Table Editor / SQL Editor / Authentication tabs **PENDING operator**.
  - Step 7–8 `.env.local` — **PASS** (all 5 expected keys present; `git check-ignore .env.local` confirms it is excluded).
  - Bonus — `npm run build` — **PASS** (Vite doesn't yet reference any `import.meta.env.VITE_*`, so the file is dormant; build still clean at 310 ms).
  - Bonus — `npm run lint` — **PASS** (exit 0).
  - Bonus — `docker ps --filter name=supabase` — **PASS** (12 containers: studio, pg_meta, edge_runtime, storage, rest, realtime, inbucket, auth, kong, analytics, db all healthy; **vector in restart loop — not on data path, will revisit**).
- Open questions for operator:
  - **Postgres version divergence (informational):** System Design §10.2 lists `postgres:16-alpine` for the VM deployment. The local Supabase CLI 2.117.0 pulled `postgres:17.6.1.167`. SQL schema and triggers in M1 are version-agnostic between 16 and 17. Pinning back to 16 would require either downgrading the CLI or pinning the image in `config.toml` (an architectural change per CLAUDE.md — needs your call before I touch it). I propose **leaving it on 17 for now** and noting the divergence in M15 (production runbook). Tell me if you'd rather pin to 16 now.
  - **Manual Test Plan visual confirmation pending:** please open `http://127.0.0.1:54321/` in a browser (expect a JSON message) and `http://127.0.0.1:54323/` (expect Studio with Table Editor / SQL Editor / Auth tabs in the left sidebar).
  - **Non-blocking:** `supabase_vector` container is in a restart loop — it's the analytics/embedding sidecar, doesn't sit on the PostgREST/Auth/Storage data path. Worth a look later but not blocking M1.



